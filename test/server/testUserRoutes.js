import supertest from 'supertest';

import app from '../../src/server';

describe('User routes', () => {
  describe('GET /users/:email', () => {
  });
  describe('POST /users/recipes', () => {
  });
  describe('DEL /users/recipes', () => {
  });
  describe('DEL /users/recipes/:id', () => {
  });
});
